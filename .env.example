# Populated by `pac code init` when this app is registered against a real
# Power Platform / Dataverse environment. Copy to .env and fill in locally;
# .env is gitignored.

# POWER_APPS_ENVIRONMENT_ID=
# POWER_APPS_APP_ID=
