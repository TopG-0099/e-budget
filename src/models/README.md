# models/

TypeScript types/interfaces that mirror Dataverse entities, e.g. `cep.ts`
exporting `Cep` and `CepStatus`. One file per entity, matching the
corresponding file in `src/services`.

No logic here, types only.
