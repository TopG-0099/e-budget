/**
 * Wraps initialization of the Power Platform Code Apps SDK.
 *
 * This app is deployed as a Power Apps Code App and runs embedded in a
 * Power Platform host, which handles authentication. Do not add custom
 * auth code here.
 *
 * TODO: once `pac code init` has been run against a real environment,
 * install `@microsoft/power-apps` and replace this stub with its
 * `initialize()` call.
 */
export async function initializePowerPlatform(): Promise<void> {
  throw new Error('initializePowerPlatform: wire up @microsoft/power-apps after `pac code init`')
}
