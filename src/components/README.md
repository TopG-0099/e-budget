# components/

Shared, reusable UI components used by more than one feature (e.g. a status
badge for CEP/CER approval state). Feature-specific components stay inside
their feature folder instead.

Empty for this MVP scaffold — no UI library has been chosen yet.
